apiVersion: v1
kind: Service
metadata:
  name: {{ include "deployment-demo.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "deployment-demo.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "deployment-demo.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
