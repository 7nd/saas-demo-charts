# Оверлей поверх values.yaml — переписывается автоматически
# .forgejo/workflows/build.yml при каждом push в app/**, не редактировать
# руками (затрётся следующим билдом). Пуст, пока CI ни разу не отработала
# — HelmRelease/app (client-infra chart) подключает оба файла через
# spec.chart.spec.valuesFiles, этот должен существовать с первого
# коммита, иначе Flux падает на несуществующем пути.
{}
